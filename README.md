# backend-node

## pre requisite
- install [Node.JS](https://nodejs.org/en/download/)
- install nvm
- install npm
- run `npm install`

## build
`npm run build`

## start the server
`npm run start`

## start the server (automatic restart)
`npm run watch` 
